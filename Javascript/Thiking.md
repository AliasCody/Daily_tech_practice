# 3 steps help me to think

- take apart the task
    - picture
    - read
    - render

## HTML

- a place to input things
- a button,and a list for the task.





# Things To Remember

1. `<input>`
    - `type` is the input should be what? (like `text`、`password`、`checkbox`)
    - `placeholder` is the text will cover it before the client input.
